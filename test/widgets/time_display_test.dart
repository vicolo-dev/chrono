import 'package:clock_app/widgets/time_display.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('time display ...', (tester) async {
    // await tester.pumpWidget(TimeDisplay(format: format, fontSize: fontSize, height: height, timezoneLocation: timezoneLocation))
  });
}
